# NeuraChat V3 Language Notes

This file documents the NeuraChat V3 support files and expected language/localization usage.

- `.env.example` contains required environment variables with blank placeholders.
- `requirements.txt` pins package versions for deployment.
- `render.yaml` contains the Render service deployment config.
